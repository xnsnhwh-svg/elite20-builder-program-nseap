# 这几天我在 NSEAP 知识库上做了什么

> 日期：2026-07-10　｜　版本：v0.4.0　｜　测试：24/24 全通过　｜　代码已推到 GitHub

---

## 先用一句话说清楚

这几天我把这个知识库从"一个只能看的静态演示"，推进成了**一个能被程序调用、有审核流程、还能带学生学习的知识服务**；并且把之前别人写的"老师带练"功能里的一堆 bug 系统性地修好了。现在测试全过，代码也都提交到 GitHub 了。

下面按"做成的几件事"来讲，每件事都说清楚**为什么做、做了什么、现在什么样**。

---

## 第一件事：让知识不只是"存着"，而是能被 AI 检索

**原来的问题**：知识库里的条目就是一堆文档，人能翻，但 AI 想用的时候不知道该调哪条、也不知道这条能干嘛。

**我做了什么**：
- 加了一个专门给 AI 用的检索接口。AI 输入一个问题，接口会按"可信度"排序返回最相关的知识（已发布的排在草稿前面），还会附上出处，方便追溯。
- 给库里 **18 条知识中的 17 条**，每条都手写了一段"AI 使用说明"——告诉 AI：什么时候该调这条、它能回答什么、不能拿它干什么。比如"学生问第一个 AI 助手怎么做时检索这条，但它不含评分细节"。

**现在什么样**：AI 检索这条链路能跑通了，我还在界面上做了个"检索预览"面板，能直接看到"AI 会检索到什么、怎么排序"。

---

## 第二件事：让知识有"生命周期"，不是写完就乱放

**原来的问题**：所有知识一个样，没法区分哪些是靠谱的、哪些还是草稿。

**我做了什么**：
- 做了一套审核流程：草稿 → 待审 → 已发布 → 废弃/归档，每一步都有记录（谁在什么时候改了什么）。
- 加了"修订历史"：每条知识改过几次、每次是什么状态，都能在界面上看到时间线。
- 把 6 条核心知识走完审核推成了"已发布"状态。

**现在什么样**：知识有了明确的状态和变更留痕，界面上一眼能看出哪条是正式的、哪条还在打磨。

---

## 第三件事：新增"评分标准（Rubric）"这一类知识

**为什么做**：将来要让 AI 帮着初评学生的作业，就得先有一份明确的评分标准，不然 AI 凭什么打分。

**我做了什么**：把"评分标准"做成了知识库里的一个正式类型，包含评分维度、每个维度的权重、每个等级长什么样（优秀/良好/及格/不足分别是什么标准）。配套的录入模板、格式规范、界面入口都做齐了。

**现在什么样**：以后写一条挑战任务，就能挂一份对应的评分标准，为"AI 自动初评"打好了地基。

---

## 第四件事（重点）：修好了"老师带练"模块的 8 个 bug

这块是别的协作者先写的，我实际跑了一遍，发现问题很多，做了一次系统性排查和修复。这也是这几天花力气最多的地方。

"老师带练"是干嘛的：它会像老师一样，带学生一步步读懂一条知识，每步问一个小问题检查是否真懂。理念很好，但实现有硬伤：

- **最严重的一个**：提示词类知识的带练，步骤被复制了两遍，学生会遇到"翻来覆去问同样问题"的鬼打墙——我删掉了重复，理清成干净的三步。
- **判分不可信**：原来只要判分接口一超时，系统就默认给满分，等于学生瞎写也算过。我改成明确提示"暂时没法判分"，并把参考答案亮出来让学生自查，不再骗分。
- **没有参考答案**：挑战和项目类的带练步骤大量缺参考答案，系统只能拿"问题本身"当答案去对，判分自然不准——我逐条补上了判断依据。
- **判分方式太糙**：没接 AI 时，原来是"看你答得字数够不够"就给分，复读一段废话也能过。我改成看回答和参考答案的"关键词重合度"，更能反映有没有真答到点上。

**现在什么样**：带练的判分闭环完整了，四种结果（对/部分对/不对/暂时没法判）都能正确处理，测试全过。

---

## 另外：数据存储做了一次底层升级

这几天协作过程中，数据存储从"纯 JSON 文件"升级成了"SQLite 数据库"作为运行时真源（JSON 保留为可审阅的镜像）。这一步是协作者完成的，我在它基础上继续开发，并确认了我的功能都正确读写新的数据层。

---

## 现在的实际状态（可核实）

- 知识条目 **18 条**，其中 17 条带完整的"AI 使用说明"。
- 状态分布：已发布 6 条 / 样例 6 条 / 草稿 6 条。
- 类型覆盖：概览、课程、挑战、提示词、FAQ、最佳实践、项目案例、Agent、评分标准。
- 自动化测试 **24 项全部通过**。
- 代码已提交并推送到 GitHub（版本 0.3.0 → 0.4.0）。

---

## 下一步打算

**近期（把 V1.0 收尾）**

1. **提示词工作室**：让用户能在库里管理、测试、复用提示词——这是 V1.0 清单里唯一还没做的正式功能。
2. **把项目自己做成一条示范知识**：用"知识认知细胞"这个项目本身当样板，展示一条结构化知识 + 配套评分标准长什么样。
3. **打通作业流程**：补上"提交—评审—复盘"这三个环节的数据结构，让"学生交作业 → AI 初评 → 教师终审"能串起来。

**想请老师确认的三件事**

1. 这个知识库当前该更偏向**给人（学生/教师）看**，还是更偏向**给其他 AI 模块调用**？这决定下一步先做哪头。
2. "AI 自动初评学生作业"这条线，这学期要不要作为重点推进？
3. 要不要把这个项目本身做成一条"知识库自我介绍"的示范条目，用于教学演示。

---

*本文档内容均来自实际代码和测试核实，不是规划设想。*

