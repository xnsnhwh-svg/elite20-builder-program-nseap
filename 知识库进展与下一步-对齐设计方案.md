# 知识库系统 · 进展与下一步（对齐设计方案）

> 汇报日期：2026-07-06
> 项目：NSEAP 知识认知细胞（nseap-knowledge-base）
> 用途：向老师同步当前进展、说明与设计方案的对齐情况、下一步计划与需要确认的问题

---

## 一、一句话定位 + 当前版本

本模块是 NSEAP 第 6 组 Knowledge Team 的知识认知细胞（Knowledge Cognitive Cell），设计目标（见 `DESIGN.md`）是做成一个**可检索、可协作、可被 Agent 调用的 Knowledge Repository**，而不是一个静态资料库。

当前状态：**V0.2 的欠账已全部清完，正踏入 V0.3（后端服务雏形）早期。** 之前几天的迭代把功能（原文优先带练、搜索、状态流转、审核、导出）做得比较靠前，但内容、数据、测试三条线掉队了；今天这一版的重点是把这三条线拉回到和功能同一水位。

## 二、对照设计方案的版本路线

以 `DESIGN.md` 第 13 章的版本路线为坐标，标出「我们在这」：

| 版本 | 目标 | 状态 |
|------|------|------|
| V0.1 静态 Demo | 验证产品模型、知识结构、演示流程 | ✅ 完成 |
| V0.2 可维护知识库 | 更多条目 + 完整 metadata + 自动索引 + 校验 + 模板 | ✅ 本次补齐 |
| **V0.3 后端服务雏形** | **API + 数据库 + 搜索 + 关系 + Markdown 导入导出** | **🔨 进行中（部分已具备）** |
| V1.0 Knowledge Repository | 正式后端 + 工作台 + 审核 + Prompt Studio + Agent 检索 + FDE 集成 | ⏳ 未开始 |

需要说明：技术能力其实已经提前踩进了 V0.3（后端 API、加权搜索、关系管理都有了），但 V0.2 该有的内容和质量此前没跟上。今天补完后，V0.2 才算真正达标，V0.3 也有了干净的起点。

## 三、这一版做了什么（相比上一版）

上一版（2026-07-05）功能超前，但底子有几处空：库里只有 10 条且数据源分裂、缺 4 类模板、关系几乎为空、8 个测试挂着。今天把这些欠账清了，每一项都挂到设计章节：

**1. 提交模板补全（对应 DESIGN 6.1）。** 从 5 类补到 8 类全覆盖，新增 overview / course / challenge / agent。其中 challenge 模板按 8.5 内置了关系示例。这样 Builder 新建任一类型都有模板可依。

**2. 数据源统一，正式知识全量入库（对应 DESIGN 9.5「Markdown 为源」）。** 此前构建产物有 14 条、后端运行库只有 10 条，两条链路各走各的，导致 `knowledge-base/` 里 12 个正式条目进不了后端 API。新增 `import-knowledge.js` 打通「md → 运行库」，把 12 条全部并入。库内 8 类内容现已齐全。这一步同时构成 V0.3 要求的 Markdown 导入能力雏形。

**3. 补显式关系与 L2 元数据（对应 DESIGN 8.5 / 8.2）。** 给违反 8.5 的干净条目补了显式关系（如 prompt → supports → challenge/course）；给 FAQ、Agent、最佳实践、概览等条目从正文提炼补了 concepts / skills。搜索和知识图谱这才有真实数据可用，不再是空壳。

**4. 清理垃圾与重复条目。** 删掉 5 条纯测试件和重复导入（如同一文件被导入两次、标题为占位符的测试条目），库净化到 17 条真实条目。

**5. 测试从 14/8 修到全绿（22/22）。** 定位出失败其实是三类根因：多数是校验规则升级后测试用例没同步、一处是重构后遗留的旧字段断言、两处是环境相关的清理钩子。逐一修复，现在全部通过。

一句话：上一版是「功能超前、内容和质量掉队」，这一版把内容、数据、测试三条线拉回到与功能同一水位。

## 四、完成度对照：还差什么（实测数据）

以下均来自对代码（`server/server.js`）和数据库（`data/knowledge-db.json`）的实际核查，非功能自述。用 DESIGN 第 14 章后端验收标准当尺子：

**已具备**：条目增删改查、按类型筛选、按关键词搜索（metadata-first 加权）、条目详情、tags/keywords/concepts/skills 维护、关系维护、Markdown/JSON 导出、Markdown 导入。

**还差三块**（都是 V0.3 → V1.0 的正常进度，不是缺陷）：

| 缺口 | 设计要求 | 现状 |
|------|---------|------|
| Agent 检索接口 | DESIGN 9.1 要求 V0.3「为 Agent 检索**预留** API」（完整版属 V1.0） | 暂无 `/api/agent/context` 端点 |
| Markdown 双向同步 | DESIGN 9.5 的 V0.3 目标是「md + 数据库双向同步」 | 导入、导出都有了，但反向的「库→md 回写」还没做 |
| 条目推到 stable | 8.7「可稳定复用」需走审核发布 | 17 条中 12 sample / 5 draft，**stable 仍为 0** |

另有一处小的口径不一致（不影响使用）：状态枚举 `allowedStatuses` 已包含 DESIGN 8.3 定义的全部 5 个（draft/review/stable/sample/deprecated）并额外加了 archived，但流转状态机目前只走通 `draft→review→stable→archived` 四态，`sample`/`deprecated` 尚未接入工作流。

## 五、下一步计划（按设计优先级排序）

目标很明确：**把 V0.3（后端服务雏形）做完整，逐条达成 DESIGN 第 14 章的 10 条验收标准。** 不再堆新的体验功能。

**第一步 — Agent 检索接口（V0.3 预留基础版）。** 做一个 `GET /api/agent/context?q=&role=&type=` 端点，复用现成的加权搜索逻辑，返回结构化、带引用的知识上下文（条目 + metadata + relationships）。注意这里只做 V0.3 要求的「预留基础版」，完整的 Agent 编排与 FDE 集成属于 V1.0，不提前。

**第二步 — Markdown 双向同步。** 导入、导出都已具备，还差反向的「库 → md 回写」，凑齐 V0.3 定义的双向同步，达成后端验收第 8 条。

**第三步 — 把主要条目推到 stable。** 走一遍 `draft → review → stable` 审核流程，让治理闭环真正跑通，也让 Agent 检索能过滤出「可信」内容，而不是全是草稿。

**第四步 — 收尾对齐。** 把状态机接上 `sample`/`deprecated`，或在设计文档中明确 `archived` 的定位，让代码与设计口径一致。

关于 KSTAR 自动判分：DESIGN 9.1 的「第一阶段不做」清单里列了「自动评分系统」，所以这项属于超前功能。建议作为「辅助功能」保留现状，不再投入扩展，把精力放回上面四步。

## 六、想请老师确认的问题

1. **服务对象定位（最想确认）**：这个知识库的核心客户，是偏学生/教师这类人类用户，还是最终服务于其他模块和 Agent 的调用？设计方案里两类角色都写了，我的理解是「近期直接服务人、终极目标是被 Agent/模块调用的知识底座」，但想请老师定调——因为这直接影响下一步优先级（若确认终极客户是 Agent，第一步的 Agent 接口权重要往前提）。

2. **优先级是否认可**：下一步先把 V0.3 做完整（Agent 预留接口 + 双向同步 + 推 stable），而不是继续做带练体验，这个方向对不对？

3. **内容来源**：`knowledge-base/` 里的真实知识条目，是我们自己按 8 类模板补，还是老师那边有现成资料可以导入沉淀？

---

## 附录：如何运行 / 演示

```bash
npm run dev          # 启动后端 + 前端，访问 http://127.0.0.1:8787/index.html
npm test             # 运行测试（当前 22/22 通过）
npm run build:index  # 从 knowledge-base/*.md 重建搜索索引与前端数据
node scripts/import-knowledge.js --update   # 把 md 内容同步进运行库
```

演示可跑通的完整链路：上传资料 → 抽取正文 → 生成草稿 → 补 metadata → 维护关系 → metadata-first 搜索 → 知识图谱 → 老师带练（原文优先）→ 状态流转与审核 → 导出 Markdown/JSON。

---

*本文档现状数据均来自对代码（`server/server.js`）和数据库（`data/knowledge-db.json`）的实际核查，非功能自述。当前库内 17 条真实条目，8 类内容齐全，测试 22/22 通过。*


